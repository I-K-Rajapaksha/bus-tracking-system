# 🏗️ System Architecture & Connections

## Complete Connection Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         AZURE CLOUD INFRASTRUCTURE                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │  Azure App Service (PHP 8.2)                                    │    │
│  │  bus-tracking-app.azurewebsites.net                             │    │
│  │                                                                  │    │
│  │  Environment Variables:                                         │    │
│  │  • DB_HOST = bus-tracking-mysql.mysql.database.azure.com       │    │
│  │  • DB_USER = bustrackadmin                                      │    │
│  │  • DB_PASS = Isu@0724                                           │    │
│  │  • DB_NAME = terminal_tracking_system                           │    │
│  │  • DB_PORT = 3306                                               │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                              ↓ SSL/TLS                                   │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │  Azure MySQL Flexible Server                                    │    │
│  │  bus-tracking-mysql.mysql.database.azure.com:3306              │    │
│  │                                                                  │    │
│  │  Database: terminal_tracking_system                             │    │
│  │  Tables: users, routes, buses, bus_arrivals,                    │    │
│  │          bus_departures, audit_logs, system_settings            │    │
│  └────────────────────────────────────────────────────────────────┘    │
│                                                                           │
└─────────────────────────────────────────────────────────────────────────┘
                              ↑
                              │ GitHub Actions (CI/CD)
                              │
┌─────────────────────────────────────────────────────────────────────────┐
│                         GITHUB REPOSITORY                                │
│  https://github.com/I-K-Rajapaksha/bus-tracking-system                  │
│                                                                           │
│  On Push to Main Branch → Automatic Deployment to Azure                  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Application Layer Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        BROWSER / USER                                    │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓ HTTPS
┌─────────────────────────────────────────────────────────────────────────┐
│                     FRONT-END LAYER                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  login.php → Authentication → Session Created                           │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────┐       │
│  │  Navigation (includes/header.php)                             │       │
│  │  • Dynamically shows menu items based on user role            │       │
│  │  • Dashboard (All roles)                                      │       │
│  │  • Terminal IN (super_admin, terminal_in_operator)            │       │
│  │  • Terminal OUT (super_admin, terminal_out_operator)          │       │
│  │  • Reports (super_admin, report_viewer)                       │       │
│  │  • Administration (super_admin only)                          │       │
│  └──────────────────────────────────────────────────────────────┘       │
│                              ↓                                            │
│  ┌──────────────────────────────────────────────────────────────┐       │
│  │  Protected Pages (require authentication + role check)        │       │
│  │                                                                │       │
│  │  dashboard.php          [All authenticated users]             │       │
│  │  modules/terminal_in/   [super_admin, terminal_in_operator]   │       │
│  │  modules/terminal_out/  [super_admin, terminal_out_operator]  │       │
│  │  modules/reports/       [super_admin, report_viewer]          │       │
│  │  modules/admin/         [super_admin only]                    │       │
│  └──────────────────────────────────────────────────────────────┘       │
│                              ↓                                            │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                     BUSINESS LOGIC LAYER                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  includes/functions.php                                                  │
│  ┌──────────────────────────────────────────────────────────────┐       │
│  │  Security Functions:                                          │       │
│  │  • isLoggedIn() - Check session                              │       │
│  │  • hasRole($role) - Verify user role                         │       │
│  │  • requireLogin() - Force authentication                     │       │
│  │  • requireRole($role) - Enforce role requirement             │       │
│  │  • sanitize($data) - Clean input                             │       │
│  │  • logAudit() - Record activity                              │       │
│  └──────────────────────────────────────────────────────────────┘       │
│                              ↓                                            │
│  ┌──────────────────────────────────────────────────────────────┐       │
│  │  Business Logic:                                              │       │
│  │  • Calculate dwell time                                       │       │
│  │  • Check if bus in terminal                                   │       │
│  │  • Generate reports                                           │       │
│  │  • Export to CSV                                              │       │
│  │  • Validate arrivals/departures                              │       │
│  └──────────────────────────────────────────────────────────────┘       │
│                              ↓                                            │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                     DATABASE ACCESS LAYER                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  includes/config.php                                                     │
│  ┌──────────────────────────────────────────────────────────────┐       │
│  │  Configuration:                                               │       │
│  │  • DB_HOST (from environment or localhost)                    │       │
│  │  • DB_USER (from environment or root)                         │       │
│  │  • DB_PASS (from environment or empty)                        │       │
│  │  • DB_NAME (from environment or terminal_tracking_system)     │       │
│  │  • DB_PORT (from environment or 3306)                         │       │
│  │  • Role constants (ROLE_SUPER_ADMIN, etc.)                    │       │
│  │  • SITE_URL (auto-detect Azure vs localhost)                 │       │
│  └──────────────────────────────────────────────────────────────┘       │
│                              ↓                                            │
│  includes/database.php                                                   │
│  ┌──────────────────────────────────────────────────────────────┐       │
│  │  Database Class (PDO):                                        │       │
│  │  • __construct() - Connect with SSL for Azure                │       │
│  │  • query($sql, $params) - Prepared statements                │       │
│  │  • single($sql, $params) - Get one row                        │       │
│  │  • resultSet($sql, $params) - Get multiple rows              │       │
│  │  • lastInsertId() - Get last inserted ID                     │       │
│  │  • beginTransaction(), commit(), rollback() - Transactions    │       │
│  └──────────────────────────────────────────────────────────────┘       │
│                              ↓ SSL/TLS Encrypted                         │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                     DATABASE LAYER                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  MySQL 8.0 (Azure Flexible Server)                                      │
│                                                                           │
│  ┌─────────────────────┐  ┌─────────────────────┐                      │
│  │ TABLES              │  │ VIEWS               │                      │
│  ├─────────────────────┤  ├─────────────────────┤                      │
│  │ • users             │  │ • vw_buses_in_      │                      │
│  │ • routes            │  │   terminal          │                      │
│  │ • buses             │  │ • vw_daily_summary  │                      │
│  │ • bus_arrivals      │  └─────────────────────┘                      │
│  │ • bus_departures    │                                                │
│  │ • audit_logs        │  ┌─────────────────────┐                      │
│  │ • system_settings   │  │ STORED PROCEDURES   │                      │
│  └─────────────────────┘  ├─────────────────────┤                      │
│                            │ • sp_record_arrival │                      │
│                            │ • sp_record_         │                      │
│                            │   departure         │                      │
│                            └─────────────────────┘                      │
│                                                                           │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Role-Based Access Control Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         USER AUTHENTICATION                              │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
                    login.php (POST username + password)
                              ↓
                    Query: SELECT * FROM users WHERE username = ?
                              ↓
                    password_verify($password, $password_hash)
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                         SESSION CREATED                                  │
│  $_SESSION['user_id'] = 1                                                │
│  $_SESSION['username'] = 'admin'                                         │
│  $_SESSION['full_name'] = 'System Administrator'                         │
│  $_SESSION['user_role'] = 'super_admin'                                  │
│  $_SESSION['last_activity'] = time()                                     │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    PAGE ACCESS REQUEST                                   │
│  User navigates to: modules/terminal_in/index.php                        │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION CHECK                                  │
│  requireRole([ROLE_SUPER_ADMIN, ROLE_TERMINAL_IN]);                     │
│                                                                           │
│  Step 1: Check if logged in                                              │
│    → if (!isLoggedIn()) → redirect to login.php                         │
│                                                                           │
│  Step 2: Check role                                                      │
│    → if (!hasRole($required_role)) → redirect to dashboard.php          │
│    → Display error: "Access denied. Insufficient permissions."          │
│                                                                           │
│  Step 3: Access granted                                                  │
│    → Page loads                                                          │
│    → User can perform allowed actions                                    │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                    SESSION TIMEOUT CHECK                                 │
│  On every page load:                                                     │
│  if (time() - $_SESSION['last_activity'] > 3600) {                      │
│    session_unset();                                                      │
│    session_destroy();                                                    │
│    redirect to login.php?timeout=1                                       │
│  }                                                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Record Bus Arrival

```
User (Terminal IN Operator) on: modules/terminal_in/index.php
                              ↓
1. Select Route → AJAX call → api/routes_api.php
                              ↓ Query: SELECT * FROM routes WHERE is_active = 1
                              ↓ Returns: JSON with route list
                              ↓
2. Select Bus → AJAX call → modules/terminal_in/index.php?ajax=get_buses
                              ↓ Query: SELECT * FROM buses WHERE route_id = ?
                              ↓ Returns: JSON with bus list
                              ↓
3. Click "Record Arrival" → AJAX POST
                              ↓
   modules/terminal_in/index.php?ajax=record_arrival
                              ↓
   Validations:
   • Check bus_number not empty
   • Check route_id valid
   • Check if bus already in terminal
                              ↓
   Insert:
   INSERT INTO bus_arrivals (
     bus_number, route_id, arrival_datetime,
     entry_method, recorded_by, ...
   ) VALUES (?, ?, NOW(), ?, ?, ...)
                              ↓
   Log:
   INSERT INTO audit_logs (
     user_id, action_type, description, ...
   ) VALUES (?, 'ARRIVAL_RECORDED', ?, ...)
                              ↓
   Response: JSON success message
                              ↓
   Front-end: Display toast notification "Bus arrival recorded successfully!"
                              ↓
   Page: Refresh terminal count, clear form
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 1: NETWORK SECURITY                                                │
│  • HTTPS enforced (Azure App Service)                                    │
│  • SSL/TLS 1.2+ for database connections                                │
│  • Azure firewall rules                                                  │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 2: AUTHENTICATION                                                  │
│  • Password hashing (bcrypt, cost 10)                                    │
│  • Session-based authentication                                          │
│  • Session timeout (1 hour)                                              │
│  • Secure session cookies                                                │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 3: AUTHORIZATION (Role-Based Access Control)                       │
│  • Page-level: requireRole() function                                    │
│  • Function-level: hasRole() checks                                      │
│  • Menu-level: Dynamic navigation filtering                              │
│  • API-level: Authentication on all endpoints                            │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 4: INPUT VALIDATION & SANITIZATION                                 │
│  • sanitize() function: htmlspecialchars + strip_tags                   │
│  • Type casting: intval(), floatval()                                    │
│  • Trim and uppercase for consistency                                    │
│  • Business logic validation                                             │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 5: SQL INJECTION PREVENTION                                        │
│  • PDO prepared statements                                               │
│  • Parameter binding                                                     │
│  • No direct string concatenation in queries                             │
│  • All queries use placeholders (?)                                      │
└─────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────────┐
│ Layer 6: AUDIT LOGGING                                                   │
│  • All actions logged to audit_logs table                                │
│  • User ID, IP address, timestamp recorded                               │
│  • Failed login attempts logged                                          │
│  • Audit trail for compliance                                            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## File Organization & Connections

```
Terminal POS/
│
├── index.php ──────────────→ Redirect to login.php or dashboard.php
│
├── login.php ──────────────→ Authentication
│   └── Connects to: users table
│   └── Creates: Session variables
│
├── logout.php ─────────────→ Session cleanup
│
├── dashboard.php ──────────→ Main overview (All roles)
│   └── Connects to: bus_arrivals, bus_departures, routes, users
│
├── includes/
│   ├── config.php ─────────→ Configuration & environment detection
│   ├── database.php ───────→ PDO wrapper with SSL support
│   ├── functions.php ──────→ Security & business logic helpers
│   ├── header.php ─────────→ Navigation (role-based filtering)
│   └── footer.php ─────────→ Closing HTML + scripts
│
├── modules/
│   ├── terminal_in/
│   │   └── index.php ──────→ Record arrivals (super_admin, terminal_in_operator)
│   │       └── Connects to: buses, routes, bus_arrivals, audit_logs
│   │
│   ├── terminal_out/
│   │   └── index.php ──────→ Record departures (super_admin, terminal_out_operator)
│   │       └── Connects to: bus_arrivals, bus_departures, routes, audit_logs
│   │
│   ├── reports/
│   │   ├── hourly.php ─────→ Hourly report (super_admin, report_viewer)
│   │   ├── daily.php ──────→ Daily report (super_admin, report_viewer)
│   │   ├── weekly.php ─────→ Weekly report (super_admin, report_viewer)
│   │   ├── monthly.php ────→ Monthly report (super_admin, report_viewer)
│   │   └── yearly.php ─────→ Yearly report (super_admin, report_viewer)
│   │       └── All connect to: bus_arrivals, bus_departures, routes
│   │
│   ├── admin/
│   │   ├── users.php ──────→ User management (super_admin only)
│   │   └── audit_logs.php ─→ View audit logs (super_admin only)
│   │       └── Connect to: users, audit_logs
│   │
│   └── master_data/
│       ├── routes.php ─────→ Route management (super_admin only)
│       └── buses.php ──────→ Bus registration (super_admin only)
│           └── Connect to: routes, buses
│
├── api/
│   ├── terminal_count.php ─→ Get bus count (authenticated users)
│   ├── routes_api.php ─────→ Route CRUD (super_admin, operators)
│   └── buses_api.php ──────→ Bus CRUD (super_admin, operators)
│       └── All require: Authentication
│
└── assets/
    ├── css/style.css
    ├── js/main.js
    └── images/
```

---

## ✅ VERIFICATION COMPLETE

**All Pages Connected to Database:** ✅  
**Role-Based Access Implemented:** ✅  
**Navigation Filtered by Role:** ✅  
**APIs Protected:** ✅  
**Session Management Active:** ✅  
**Security Layers Applied:** ✅  
**Audit Logging Enabled:** ✅  

**System Status:** FULLY OPERATIONAL with proper role-based access control

---

*System Architecture Documentation*  
*Bus Tracking System - Makumbura MMC*  
*Version 1.0.0*  
*February 1, 2026*
